<!-- 复制本文件为 docs/hermes/<主题>.md，按下列骨架填写后删除本行注释。骨架源自 AGENT-OUTBOUND-CALL.md。 -->

# <主题>（Hermes 代码梳理）

> 目标读者：<谁要用这篇、解决什么问题>。
> 范围：<覆盖什么；明确**不**覆盖什么，并指向覆盖它的其他篇>。
> 基于：<日期 / commit / 分支> 核查；源码根与引用约定见 [README.md](README.md)。
> 证据约定：每条结论尽量带「路径:行号」；推断标「未在代码中找到，推断」，落地前实测确认。

---

## 1. 一句话概述 + 时序/结构图

<一句话讲清这条链路 / 这个模块在做什么>

```
<ASCII 时序图（链路类）或模块依赖图（模块类）>
```

## 2. 逐环节 / 逐模块说明（带证据）

### 2.1 <环节/模块>
- **结论**：……
- **证据**：`模块/路径/File.kt:行号`。
- <要点 / 坑 / 与直觉不同处>

### 2.2 ……

## 3. 关键接口 / 数据结构 / 信令

<HTTP 接口表、SIP/ESL 头、DTO、事件名——用表格汇总，每行带证据路径>

## 4. 对 hermes-mock 的启示

- **可复用 / 已具备**：……
- **做不到 / 需外部依赖 / 需桩**：……

## 5. 待补 / 推断项

- ⚠️ ……（标明哪些是推断、哪些待实测）

## 附：关键证据索引（路径:行号）

- `模块/路径/File.kt:行号`：<这条证明了什么>
